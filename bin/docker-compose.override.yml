version: '3.4'

services:
  tests:
    build:
      context: ./
      target: ci
    volumes:
      - ${WORKSPACE}/reports:/app/reports
    environment:
      - DJANGO_SETTINGS_MODULE=ztc.conf.ci
      - SECRET_KEY=jenkins
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASSWORD=
      - DB_HOST=db
      - WORKSPACE=${WORKSPACE}
    depends_on:
      - db
